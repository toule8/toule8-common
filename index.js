'use strict';

const utils = require('./lib/utils'),
  wechat = require('./lib/wechat'),
  config = require('./lib/config');

module.exports = {
  utils: utils,
  wechat: wechat,
  users: users,
  config: config
};
